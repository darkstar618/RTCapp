# Dev-only load testing scripts

These scripts are for local development and staging validation only.
Do not run against production without explicit approval.

Requires the API server running at `RTC_API_URL` (default `http://localhost:3002`).
